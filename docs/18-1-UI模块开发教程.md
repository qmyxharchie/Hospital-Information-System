# 18-1 UI模块开发教程

> 本文档为学习版，内容以实现思路为主，关键处给出可参考的核心片段
> 参考源码路径：`hospital-management/src/ui.c`、`hospital-management/include/ui.h`、`hospital-management/src/main_gui.cpp`

---

## 1. 模块概述

### 1.1 功能描述

UI界面模块负责整个医院信息管理系统的用户交互层，同时提供两套界面：

| 界面类型 | 源文件 | 说明 |
|---------|--------|------|
| 控制台版本 | `src/ui.c` | 命令行菜单，适合学习期间调试 |
| 图形界面版本 | `src/main_gui.cpp` | 基于 StellarX，适合实际使用 |

### 1.2 界面模块与其他模块的关系

```
┌─────────────────────────────────────────────────────┐
│              UI 界面模块 (ui.c / main_gui.cpp)      │
│  - 负责显示菜单、接收用户输入并调用业务层函数        │
│  - 不做数据运算，只做"展示"与"交互"                 │
└─────────────────────────────────────────────────────┬─
                   │ 调用各模块函数（patient.c、doctor.c …）
                   │
┌─────────────────────────────────────────────────────┬─
│              业务逻辑模块 (各模块 .c 文件)            │
│  - 增删改查操作                                   │
│  - 链表操作                                       │
└─────────────────────────────────────────────────────┬─
                   │ 调用文件读写函数
                   │
┌─────────────────────────────────────────────────────┐
│              数据持久化 (file_io.c)                │
│  - 从 data/*.txt 读入链表                          │
│  - 从链表写入 data/*.txt                           │
└─────────────────────────────────────────────────────┘
```

### 1.3 文件清单

| 目标路径 | 说明 |
|---------|------|
| `include/ui.h` | 头文件：声明、颜色宏、尺寸宏 |
| `src/ui.c` | 控制台版本实现 |
| `src/main_gui.cpp` | 图形界面版本实现 |

---

## 2. ui.h 头文件创建指南

### 2.1 创建步骤

**步骤1**：创建 `include/ui.h` 文件
- 路径：`Hospital-Information-System/include/ui.h`
- 编码：UTF-8
- 文件类型：C语言头文件

**步骤2**：添加头文件保护宏
- 防止重复包含
- 使用标准格式

**步骤3**：定义颜色宏
- 用于GUI界面的颜色定义
- 使用RGB函数格式

**步骤4**：定义尺寸宏
- 用于GUI界面的尺寸定义
- 统一界面元素大小

**步骤5**：声明全局链表指针
- 声明为extern，供UI模块使用
- 便于访问各业务模块数据

**步骤6**：声明UI函数原型
- 定义所有UI相关函数的原型
- 包含参数和返回值说明

### 2.2 颜色定义实现思路

```c
// 颜色定义示例格式
#define COLOR_BG           RGB(240, 248, 255)  // 背景色
#define COLOR_TITLE        RGB(70, 130, 180)    // 标题色
#define COLOR_TEXT         RGB(50, 50, 50)      // 文本色
// ... 更多颜色定义
```

**实现提示**：
- RGB值可根据界面设计调整
- 颜色命名应具有描述性
- 保持颜色方案一致性

### 2.3 尺寸定义实现思路

```c
// 尺寸定义示例格式
#define WINDOW_WIDTH   1000   // 窗口宽度
#define WINDOW_HEIGHT  700    // 窗口高度
// ... 更多尺寸定义
```

**实现提示**：
- 尺寸值应适合目标显示设备
- 保持界面元素比例协调
- 考虑响应式设计需求

### 2.4 全局链表指针声明实现思路

```c
// 全局链表指针声明示例格式
extern Patient*         g_patientHead;
extern Patient*         g_patientTail;
extern Doctor*          g_doctorHead;
// ... 更多指针声明
```

**实现提示**：
- 使用extern关键字声明
- 指针在对应模块中定义
- UI模块只使用不定义

---

## 3. 控制台版 ui.c 创建指南

### 3.1 创建步骤

**步骤1**：创建 `src/ui.c` 文件
- 路径：`Hospital-Information-System/src/ui.c`
- 编码：UTF-8
- 文件类型：C语言源文件

**步骤2**：包含必要的头文件
- 包含标准库头文件
- 包含项目自定义头文件

**步骤3**：实现UI初始化函数
- 初始化界面相关资源
- 为GUI版本预留接口

**步骤4**：实现菜单显示函数
- 实现各级菜单显示
- 使用美观的边框格式

**步骤5**：实现交互处理函数
- 处理用户输入
- 调用相应业务函数

### 3.2 登录页面实现思路

**实现步骤**：
1. **步骤1**：打印登录菜单界面（用 `┌ ┐ └ │ ║` 画框）
2. **步骤2**：`scanf("%d", &choice)` 接收用户选择
3. **步骤3**：`getchar()` 清除输入缓冲区残留的换行符
4. **步骤4**：`switch(choice)` 分支处理：
   - `case 1` ： 提示输入账号密码，调用 `login()` 函数
   - `case 2` ： 同上（管理员入口，最终也调用 `login()`）
   - `case 3` ： 调用 `registerUser()` 注册新用户
   - `case 0` ： 返回 `LOGIN_EXIT`
5. **步骤5**：返回对应的登录状态码

**核心片段参考**：
```c
int showLoginPage(void) {
    // 实现思路：
    // 1. 打印带边框的登录界面
    // 2. 接收用户选择
    // 3. 根据选择执行相应操作
    // 4. 调用登录函数验证
    // 5. 返回登录状态
}
```

**实现提示**：
- 使用字符绘制美观的界面边框
- 输入处理要考虑缓冲区清理
- 状态码返回要与登录模块匹配

### 3.3 主菜单实现思路

**实现步骤**：
1. **步骤1**：打印主菜单框（8个功能选项 + 退出）
2. **步骤2**：`scanf("%d", &module)` 接收模块选择
3. **步骤3**：`switch(module)` 跳转到对应模块菜单函数

**核心片段参考**：
```c
void showMainMenu(void) {
    // 实现思路：
    // 1. 打印主菜单界面
    // 2. 显示所有功能选项
    // 3. 提供清晰的导航指示
}
```

**实现提示**：
- 保持界面风格一致
- 功能选项布局合理
- 提供明确的退出选项

### 3.4 功能模块菜单实现思路

**通用实现模式**（以病人管理为例）：
```c
void showPatientManagement(void) {
    // 实现思路：
    // 1. while(1) 循环显示菜单，直到用户选 0 返回
    // 2. scanf("%d", &choice) + getchar() 接收选择
    // 3. switch(choice) 分支处理：
    //    - case 1（添加）： 提示输入各字段 → 调用 addPatient() → 调用 rebuildPatientFile() 写文件
    //    - case 2（删除）： 输入卡号 → findPatientByCardNo() 查找 → delPatient() 删除 → 写文件
    //    - case 3（修改）： 查找后修改指定字段 → 写文件
    //    - case 4（查找）： 按卡号查找并打印
    //    - case 5（列表）： 调用 listAllPatients() 遍历打印
    //    - case 0 ： return 退出当前函数
    // 4. 每个增删改操作后都需 rebuildPatientFile() 同步写回磁盘
}
```

**实现提示**：
- 保持菜单结构一致性
- 每次修改后都要保存数据
- 错误处理要及时反馈

---

## 4. 图形界面版本 main_gui.cpp 创建指南

### 4.1 创建步骤

**步骤1**：创建 `src/main_gui.cpp` 文件
- 路径：`Hospital-Information-System/src/main_gui.cpp`
- 编码：UTF-8
- 文件类型：C++源文件
- 注意：使用WinMain入口函数

**步骤2**：包含StellarX框架头文件
- 包含必要的GUI框架头文件
- 确保框架路径正确

**步骤3**：实现系统初始化函数
- 从文件加载所有数据到链表
- 确保数据一致性

**步骤4**：创建主窗口对象
- 设置窗口大小和标题
- 配置窗口属性

**步骤5**：创建界面元素
- 实现登录页面
- 实现主功能页面

### 4.2 框架介绍 — StellarX

本项目图形界面基于 **StellarX** 框架开发（GitHub: `https://github.com/Ysm-04/StellarX`）。

**核心特点**：
- 基于 EasyX 的 C++ GUI 框架，组件化设计
- 使用 `std::unique_ptr` 管理控件生命周期
- 事件绑定使用 Lambda 表达式（`setOnClickListener([&](){ ... })`）
- 入口函数为 `WinMain()`，不使用 `main()`

**常用组件速查**：

| 组件 | 作用 | 对应控制台操作 |
|------|------|--------------|
| `Window` | 主窗口容器 | 无 |
| `Label` | 文字标签 | `printf` 输出文字 |
| `Button` | 按钮 | 菜单选项选择 |
| `TextBox` | 文本输入框 | `scanf` 输入 |
| `Table` | 表格（显示列表） | `listAllXxx()` 遍历打印 |
| `Canvas` | 页面容器 | 每个功能模块专用 Canvas |
| `TabControl` | 左侧导航栏 | 主菜单各项切换 |
| `MessageBox` | 弹出消息框 | `printf` 提示信息 |

### 4.3 窗口和入口实现思路

**实现思路**：
1. **步骤1**：调用 `initSystem()` 从文件加载所有数据到链表
2. **步骤2**：创建 `Window` 主窗口
3. **步骤3**：创建登录页 Canvas（控件 + 背景图）
4. **步骤4**：创建主页面 TabControl（含各模块页面）
5. **步骤5**：绑定登录按钮事件（调用 `login()` 函数）
6. **步骤6**：调用 `mainWindow.draw()` 绘制，`runEventLoop()` 启动循环
7. **步骤7**：窗口关闭后调用 `exitSystem()` 保存并释放

**核心片段参考**：
```cpp
int WINAPI WinMain(HINSTANCE, HINSTANCE, LPSTR, int)
{
    // 实现思路：
    // 1. 初始化系统数据
    // 2. 创建主窗口对象
    // 3. 构建界面元素
    // 4. 设置事件处理
    // 5. 启动消息循环
    // 6. 清理资源
}
```

**实现提示**：
- 使用WinMain而非main函数
- 确保资源正确释放
- 注意C++和C混合编译问题

### 4.4 登录页面实现思路

**实现思路**：
1. **步骤1**：创建登录 Canvas 容器，设置透明背景
2. **步骤2**：创建 `Label`（标题"欢迎使用" + 账号/密码标签）
3. **步骤3**：创建两个 `TextBox`（账号输入框、密码输入框），设置最大字符数
4. **步骤4**：创建两个 `Button`："用户登录"和"管理员登录"
5. **步骤5**：所有控件加入 Canvas，Canvas 加入 Window

**核心片段参考**：
```cpp
// 实现思路：
// 1. 创建Canvas容器
// 2. 添加Label控件（标题和标签）
// 3. 添加TextBox控件（输入框）
// 4. 添加Button控件（登录按钮）
// 5. 设置控件属性和布局
// 6. 绑定事件处理函数
```

**实现提示**：
- 控件布局要合理美观
- 输入框要限制字符长度
- 事件绑定要正确处理

### 4.5 主页面实现思路

**实现思路**：
1. **步骤1**：创建 `TabControl`，设置标签在左侧（`TabPlacement::Left`）
2. **步骤2**：为每个模块创建一对：`Button`（标签）+ `Canvas`（页面内容）
3. **步骤3**：每个 Canvas 上放 `Table`（数据表格）+ 若干 `Button`（操作按钮）
4. **步骤4**：调用 `tabControl.add(pair)` 把每个模块页面加进去
5. **步骤5**：初始 `setIsVisible(false)` 隐藏，登录成功后改为 `true`

**核心片段参考**：
```cpp
// 实现思路：
// 1. 创建TabControl控件
// 2. 为每个功能模块创建页面
// 3. 在页面上添加表格和按钮
// 4. 将页面添加到TabControl
// 5. 设置页面可见性
```

**实现提示**：
- TabControl布局要清晰
- 表格列头要明确标识
- 操作按钮功能要明确

### 4.6 事件处理实现思路

**登录事件实现思路**：
1. **步骤1**：调用 `textBox->getText()` 获取用户输入（返回 `std::string`）
2. **步骤2**：空值检查——若账号或密码为空，调用 `setTextBoxBk(RGB(255,0,0))` 变红提示
3. **步骤3**：调用 `login((char*)username.c_str(), (char*)password.c_str())`
4. **步骤4**：登录成功 — 隐藏登录 Canvas (`setIsVisible(false)`)、显示主页 (`setIsVisible(true)`)、切换背景图
5. **步骤5**：失败 — `MessageBox::showModal()` 弹出提示

**核心片段参考**：
```cpp
// 实现思路：
// 1. 获取输入框内容
// 2. 验证输入有效性
// 3. 调用登录验证函数
// 4. 根据结果切换界面状态
// 5. 显示相应提示信息
```

**实现提示**：
- 输入验证要及时反馈
- 界面切换要平滑自然
- 错误提示要友好明确

---

## 5. 开发注意事项

### 5.1 控制台版 vs 图形版选择

| 场景 | 推荐版本 |
|------|---------|
| 学习数据结构（链表增删改查） | 控制台版 `ui.c` |
| 快速验证功能逻辑 | 控制台版 |
| 实际项目展示 | 图形版 `main_gui.cpp` |

### 5.2 数据一致性原理

> **核心原则：增删改后必须同步写回文件**

```
内存链表（g_xxxHead）
      ↑
      ↑  rebuildXxxFile(g_xxxHead)   ↑ 操作后必须调用
      ↑
磁盘文件（data/xxx.txt）
```

每次 `addXxx()` / `delXxx()` / `modifyXxx()` 之后，必须调用 `rebuildXxxFile()` 将修改写回磁盘，否则程序重启后数据丢失。

### 5.3 输入安全

- `scanf("%s", buf)` 应改为 `scanf("%49s", buf)`，防止缓冲区溢出
- `scanf()` 后加 `getchar()` 清除换行符，避免影响下一次输入
- 密码输入用 `getch()` 实时读取字符不回显

### 5.4 GUI 调试建议

1. 先确保控制台版功能正确，再迁移到 GUI
2. GUI 中复杂操作（增删改）暂时跳转到控制台完成，GUI 仅做展示
3. `WinMain` 中可以加 `printf` 语句，通过 Visual Studio 的输出窗口查看调试信息

## 6. 创建文件检查清单

### 6.1 ui.h 创建检查
- [ ] 文件已创建：`include/ui.h`
- [ ] 包含保护宏
- [ ] 颜色宏定义
- [ ] 尺寸宏定义
- [ ] 全局指针声明
- [ ] 函数原型声明

### 6.2 ui.c 创建检查
- [ ] 文件已创建：`src/ui.c`
- [ ] 包含必要的头文件
- [ ] 实现登录页面函数
- [ ] 实现主菜单函数
- [ ] 实现各功能模块菜单
- [ ] 数据操作后保存文件

### 6.3 main_gui.cpp 创建检查
- [ ] 文件已创建：`src/main_gui.cpp`
- [ ] 使用WinMain入口
- [ ] 包含StellarX头文件
- [ ] 实现系统初始化
- [ ] 创建主窗口
- [ ] 实现登录页面
- [ ] 实现主功能页面
- [ ] 绑定事件处理

## 7. 下一步学习

1. [12-文件IO_](./12-文件IO_.md) — 数据如何持久化存储
2. [05-病人模块_](./05-病人模块_.md) — 病人模块数据操作
3. [10-登录模块_](./10-登录模块_.md) — 登录与权限控制

---

**文档版本**：v1.0（学习版 · 实现思路引导 · 参考 `hospital-management` 源码）