# 住院模块 - 预交金追加 & 住院信息修改补充教程

> 经核查 `src/hospitalization.c`，当前住院模块**仅包含**入院登记（`addHospitalization`）和出院结算（`dischargePatient`），**不包含**以下功能：
> - 追加/补交预交金
> - 修改住院信息（如转床、追加押金等）
>
> 本文档补充这两个缺失功能的实现。

---

## 1. 当前住院模块状态

| 功能 | 状态 | 所在函数 |
|------|------|----------|
| 入院登记 | ✅ 已有 | `addHospitalization()` |
| 出院结算 | ✅ 已有 | `dischargePatient()` |
| 按卡号查询 | ✅ 已有 | `findHospitalizationByCardNo()` |
| 按住院单号查询 | ✅ 已有 | `findHospitalizationByNo()` |
| 当前在院病人 | ✅ 已有 | `findAllCurrentHospitalizations()` |
| **追加预交金** | ❌ **缺失** | 需补充 `addPrepay()` |
| **修改住院信息** | ❌ **缺失** | 需补充 `modifyHospitalization()` |

---

## 2. 头文件声明补充

在 `include/hospitalization.h` 中，现有声明之后追加以下内容：

```c
// 以下为新增声明 —— 预交金追加与住院信息修改
void addPrepay(Hospitalization* h, double amount);
    // 追加预交金（amount > 0）

int modifyHospitalization(Hospitalization* head, char* recordNo,
    char* newBedNo, double addPrepayAmount);
    // 修改住院信息：转床 + 追加预交金
    // 返回 1=成功，0=失败
```

---

## 3. 追加预交金 — addPrepay()

### 3.1 实现思路

1. **边界检查**：`h == NULL` 则返回
2. **累加预交金**：`h->data.prepay += amount`
3. **输出提示**：显示追加金额和当前总预交金
4. **写回文件**：`rebuildHospitalizationFile(g_hosHead)`（全局头指针）

> **注意**：`dischargePatient()` 中已有 `returnCost = prepay - totalCost` 的逻辑，预交金追加后出院结算会自动使用最新预交金。

### 3.2 完整代码

```c
//-------------------------
// 以下为追加预交金函数
// 功能：为指定住院记录追加预交金
// 参数：h - 住院记录节点指针，amount - 追加金额（必须 > 0）
//-------------------------
void addPrepay(Hospitalization* h, double amount)
{
    // 1. 边界检查
    if (h == NULL) {
        printf("[ERROR] 住院记录为空，无法追加预交金。\n");
        return;
    }

    // 2. 检查追加金额合法性
    if (amount <= 0) {
        printf("[ERROR] 追加金额必须大于 0。\n");
        return;
    }

    // 3. 检查患者是否仍在院
    if (strcmp(h->data.status, "住院") != 0) {
        printf("[ERROR] 该患者已出院，无法追加预交金。\n");
        return;
    }

    // 4. 累加预交金
    h->data.prepay += amount;

    // 5. 写回文件（通过全局头指针）
    extern Hospitalization* g_hosHead;
    rebuildHospitalizationFile(g_hosHead);

    // 6. 输出提示
    printf("[OK] 预交金追加成功！\n");
    printf("  本次追加：%.2f 元\n", amount);
    printf("  当前预交金总额：%.2f 元\n", h->data.prepay);
}
```

---

## 4. 修改住院信息 — modifyHospitalization()

### 4.1 实现思路

此函数整合两种常见操作：**转床**和**追加预交金**。参数中 `newBedNo` 为空字符串 `""` 表示不转床；`addPrepayAmount` 为 `0` 表示不追加。

1. **按住院单号查找**
2. **找不到则返回 0**
3. **转床处理**（如需要）：
   - 释放旧床位（`freeBed()`）
   - 分配新床位（`allocateBed()`）
   - 更新 `bedNo`
4. **追加预交金**（如需要）
5. **写回文件**
6. **返回 1**

### 4.2 完整代码

```c
//-------------------------
// 以下为修改住院信息函数
// 功能：修改指定住院记录（转床 + 追加预交金）
// 参数：head - 住院链表头指针
//      recordNo - 住院单号
//      newBedNo - 新床位号（为空字符串则不转床）
//      addPrepayAmount - 追加预交金金额（为 0 则不追加）
// 返回值：1 - 成功，0 - 失败（未找到或已出院）
//-------------------------
int modifyHospitalization(Hospitalization* head, char* recordNo,
    char* newBedNo, double addPrepayAmount)
{
    // 1. 按住院单号查找记录
    Hospitalization* h = findHospitalizationByNo(head, recordNo);

    // 2. 检查是否找到
    if (h == NULL) {
        printf("[ERROR] 未找到住院单号为 %s 的记录。\n", recordNo);
        return 0;
    }

    // 3. 检查是否已出院
    if (strcmp(h->data.status, "住院") != 0) {
        printf("[ERROR] 该患者已出院，无法修改住院信息。\n");
        return 0;
    }

    // 4. 显示当前信息
    printf("当前住院信息：\n");
    printf("  住院单号：%s\n", h->data.recordNo);
    printf("  患者姓名：%s\n", h->data.patientName);
    printf("  当前床位：%s\n", h->data.bedNo);
    printf("  当前预交金：%.2f 元\n", h->data.prepay);

    // 5. 转床处理（如果 newBedNo 非空）
    if (newBedNo != NULL && strlen(newBedNo) > 0) {
        // 释放旧床位
        Bed* oldBed = findBedByNo(g_bedHead, h->data.bedNo);
        if (oldBed != NULL) {
            freeBed(oldBed);
            printf("  [OK] 旧床位 %s 已释放。\n", h->data.bedNo);
        }

        // 分配新床位
        Bed* newBed = findBedByNo(g_bedHead, newBedNo);
        if (newBed == NULL) {
            printf("[ERROR] 床位 %s 不存在。\n", newBedNo);
            return 0;
        }
        if (strcmp(newBed->data.status, "空闲") != 0) {
            printf("[ERROR] 床位 %s 非空闲状态，无法分配。\n", newBedNo);
            return 0;
        }

        // 占用新床位
        strcpy(newBed->data.patientCardNo, h->data.patientCardNo);
        strcpy(newBed->data.patientName, h->data.patientName);
        strcpy(newBed->data.status, "占用");
        strcpy(h->data.bedNo, newBedNo);

        printf("  [OK] 已成功转至床位 %s。\n", newBedNo);

        // 写回床位文件
        rebuildBedFile(g_bedHead);
    }

    // 6. 追加预交金（如果金额 > 0）
    if (addPrepayAmount > 0) {
        h->data.prepay += addPrepayAmount;
        printf("  [OK] 预交金追加 %.2f 元，当前总额 %.2f 元。\n",
               addPrepayAmount, h->data.prepay);
    }

    // 7. 写回住院记录文件
    rebuildHospitalizationFile(head);

    printf("[OK] 住院信息修改成功。\n");
    return 1;
}
```

---

## 5. UI 菜单联动修改

在 `docs/18-1-UI模块开发教程代码.md` 的 `showHospitalizationManagement()` 中，现有选项：

```
1. 入院登记       4. 按住院单号查询
2. 出院结算       5. 显示当前在院病人
3. 按卡号查询     6. 显示所有住院记录
0. 返回上级菜单
```

建议改为：

```
1. 入院登记         5. 显示当前在院病人
2. 出院结算         6. 显示所有住院记录
3. 按卡号查询       7. 追加预交金
4. 按住院单号查询   8. 修改住院信息（转床/追加押金）
0. 返回上级菜单
```

在 `switch` 中补充 `case 7` 和 `case 8`：

```c
case 7: {  // 追加预交金
    printf("请输入住院单号: ");  scanf("%19s", recordNo);
    printf("请输入追加金额: ");  scanf("%lf", &prepay);
    Hospitalization* h = findHospitalizationByNo(g_hosHead, recordNo);
    if (h) {
        addPrepay(h, prepay);
    } else {
        printf("[ERROR] 未找到该住院记录！\n");
    }
    break;
}

case 8: {  // 修改住院信息（转床 + 追加预交金）
    printf("请输入住院单号: ");     scanf("%19s", recordNo);
    printf("请输入新床位号(不转床输0): ");  scanf("%19s", bedNo);
    printf("请输入追加预交金(不追加输0): "); scanf("%lf", &prepay);
    if (strcmp(bedNo, "0") == 0) {
        strcpy(bedNo, "");        // 空字符串表示不转床
    }
    modifyHospitalization(g_hosHead, recordNo, bedNo, prepay);
    break;
}
```

---

## 6. 对现有 addHospitalization() 的一处修正建议

当前 `addHospitalization()` 中第 43 行：

```c
strcpy(h->data.prepay, prepay);   // ❌ 错误：prepay 是 double，不能用 strcpy
```

应改为：

```c
h->data.prepay = prepay;           // ✅ 正确：数值类型直接赋值
```

同理，`dischargePatient()` 中第 82 行的 `printf` 建议增加换行：

```c
// 原代码
printf("补交费用 %f 元", (-1) * returnCost);

// 建议修改为
printf("补交费用 %.2f 元\n", (-1) * returnCost);
```

---

> **验证方式**：
> 1. 先 `addHospitalization()` 办理入院
> 2. `findHospitalizationByNo()` 查询确认
> 3. `addPrepay()` 追加预交金，观察 `data/hospitalization.txt` 中 prepay 字段是否累加
> 4. `modifyHospitalization()` 转床，观察 `data/bed.txt` 中旧床位变空闲、新床位变占用
> 5. `dischargePatient()` 出院结算，验证 `returnCost = 最新 prepay - totalCost`
