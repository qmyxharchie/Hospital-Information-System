# 挂号系统详细实现 - 预约挂号与现场挂号

## 1. 挂号系统概述

挂号系统是医疗信息系统的核心模块之一，负责管理患者与医生之间的预约关系。系统支持两种挂号方式：
- 预约挂号：提前预约未来的就诊时间
- 现场挂号：当天即时挂号

## 2. 数据结构定义

### 2.1 挂号记录结构体 (registration.h)

```c
#ifndef REGISTRATION_H
#define REGISTRATION_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>
#include "data_structures.h"  // 包含UserType定义
#include "doctor.h"          // 医生模块
#include "patient.h"         // 患者模块
#include "utils.h"           // 工具函数

// 挂号状态枚举
typedef enum {
    REG_PENDING = 0,    // 待就诊
    REG_IN_PROGRESS = 1, // 就诊中
    REG_COMPLETED = 2,   // 已完成
    REG_CANCELLED = 3    // 已取消
} RegStatus;

// 挂号信息结构体
typedef struct RegistrationData {
    char regNo[20];              // 挂号单号（如"REG001"）
    char patientCardNo[20];      // 病人卡号
    char patientName[50];        // 病人姓名（冗余存储，便于显示）
    char doctorEmpNo[20];        // 医生工号
    char doctorName[50];         // 医生姓名（冗余存储）
    char dept[50];               // 科室
    char date[20];               // 挂号日期（格式：YYYY-MM-DD）
    char time[20];               // 挂号时间（格式：HH:MM:SS）
    char appointmentDate[20];    // 预约日期（格式：YYYY-MM-DD，如果是现场挂号则与挂号日期相同）
    char appointmentTime[20];    // 预约时间（格式：HH:MM，如果是现场挂号则与挂号时间相同）
    int status;                  // 状态（0-待就诊, 1-就诊中, 2-已完成, 3-已取消）
    UserType createdBy;          // 挂号方式（PATIENT-预约挂号, NURSE-现场挂号）
} RegistrationData;

typedef struct Registration {
    RegistrationData data;
    struct Registration* next;
    struct Registration* pre;
} Registration;

// 函数声明
Registration* getRegistrationHead(void);                           // 获取挂号链表头
Registration* getRegistrationTail(void);                          // 获取挂号链表尾

// 挂号操作
int addRegistration(Registration** head, Registration** tail,
    char* patientCardNo, char* patientName,
    char* doctorEmpNo, char* doctorName, char* dept,
    char* appointmentDate, char* appointmentTime, UserType creator); // 添加挂号记录

int cancelRegistration(Registration** head, Registration** tail,
    Registration* r, UserType user_type, int user_id);            // 取消挂号
int completeRegistration(Registration* r, UserType user_type);    // 完成就诊
int callNextPatient(Registration** head, Registration** tail, 
    char* doctorEmpNo, UserType user_type);                      // 叫号

// 查询函数
Registration* findRegistrationByNo(Registration* head, char* regNo); // 按挂号单号查询
Registration* findRegistrationsByPatient(Registration* head, char* patientCardNo); // 按病人查询
Registration* findRegistrationsByDoctor(Registration* head, char* doctorEmpNo); // 按医生查询
Registration* findRegistrationsByDateRange(Registration* head, int y1,
    int m1, int d1, int y2, int m2, int d2);                     // 按日期范围查询
Registration* findRegistrationsByStatus(Registration* head, int status); // 按状态查询

// 列表显示
void listAllRegistrations(Registration* head);                    // 显示所有挂号
void listRegistrationsByDoctor(Registration* head, char* doctorEmpNo); // 显示医生的挂号列表
void listRegistrationsByPatient(Registration* head, char* patientCardNo); // 显示患者的挂号列表

#endif
```

## 3. 挂号功能实现

### 3.1 预约挂号实现

```c
/**
 * 预约挂号函数
 * @param head 挂号链表头指针的指针
 * @param tail 挂号链表尾指针的指针
 * @param patientCardNo 患者卡号
 * @param patientName 患者姓名
 * @param doctorEmpNo 医生工号
 * @param doctorName 医生姓名
 * @param dept 科室
 * @param appointmentDate 预约日期 (YYYY-MM-DD)
 * @param appointmentTime 预约时间 (HH:MM)
 * @param creator 挂号创建者类型
 * @return 成功返回1，失败返回0
 */
int addRegistration(Registration** head, Registration** tail,
    char* patientCardNo, char* patientName,
    char* doctorEmpNo, char* doctorName, char* dept,
    char* appointmentDate, char* appointmentTime, UserType creator) {
    
    // 1. 验证医生是否存在
    Doctor* d = findDoctorByEmpNo(getDoctorHead(), doctorEmpNo);
    if (d == NULL) {
        printf("未找到对应医生\n");
        return 0;
    }
    
    // 2. 检查医生是否还有名额
    if (!canAcceptPatient(d)) {
        printf("该医生当日号源已满\n");
        return 0;
    }
    
    // 3. 检查同一名患者是否已有同一天同一医生的待就诊挂号
    Registration* temp = *head;
    while(temp != NULL) {
        if(strcmp(temp->data.patientCardNo, patientCardNo) == 0 && 
           strcmp(temp->data.doctorEmpNo, doctorEmpNo) == 0 &&
           strcmp(temp->data.appointmentDate, appointmentDate) == 0 &&
           (temp->data.status == REG_PENDING || temp->data.status == REG_IN_PROGRESS)) {
            printf("该患者在该医生处已有待就诊或就诊中的挂号记录\n");
            return 0;
        }
        temp = temp->next;
    }
    
    // 4. 分配新节点
    Registration* newNode = (Registration*)malloc(sizeof(Registration));
    if (newNode == NULL) {
        printf("内存分配失败\n");
        return 0;
    }
    
    // 5. 初始化节点指针
    newNode->next = newNode->pre = NULL;
    
    // 6. 生成挂号单号
    char id[20];
    generateUniqueId("REG", id);
    strcpy(newNode->data.regNo, id);
    
    // 7. 填充挂号信息
    strcpy(newNode->data.patientCardNo, patientCardNo);
    strcpy(newNode->data.patientName, patientName);
    strcpy(newNode->data.doctorEmpNo, doctorEmpNo);
    strcpy(newNode->data.doctorName, doctorName);
    strcpy(newNode->data.dept, dept);
    
    // 8. 获取当前日期和时间作为挂号时间
    int year, month, day, hour, minute, second;
    time_t now = time(0);
    struct tm* timeinfo = localtime(&now);
    
    year = timeinfo->tm_year + 1900;
    month = timeinfo->tm_mon + 1;
    day = timeinfo->tm_mday;
    hour = timeinfo->tm_hour;
    minute = timeinfo->tm_min;
    second = timeinfo->tm_sec;
    
    sprintf(newNode->data.date, "%04d-%02d-%02d", year, month, day);
    sprintf(newNode->data.time, "%02d:%02d:%02d", hour, minute, second);
    
    // 9. 设置预约日期和时间
    strcpy(newNode->data.appointmentDate, appointmentDate);
    strcpy(newNode->data.appointmentTime, appointmentTime);
    
    // 10. 设置初始状态为待就诊
    newNode->data.status = REG_PENDING;
    
    // 11. 记录挂号方式
    newNode->data.createdBy = creator;
    
    // 12. 尾插法插入链表
    if (*tail == NULL) {
        *head = newNode;
        *tail = newNode;
    } else {
        (*tail)->next = newNode;
        newNode->pre = *tail;
        *tail = newNode;
    }
    
    // 13. 增加医生的接诊人数（仅对当天的预约）
    if(strcmp(appointmentDate, newNode->data.date) == 0) {
        // 如果是当天挂号，则增加当天接诊人数
        incrementPatientCount(d);
    }
    
    // 14. 保存到文件
    rebuildRegistrationFile(*head);
    
    // 15. 输出挂号成功信息
    if(creator == PATIENT) {
        printf("预约挂号成功！挂号编号：%s，患者：%s，医生：%s，科室：%s，预约日期：%s %s\n",
            newNode->data.regNo, newNode->data.patientName,
            newNode->data.doctorName, newNode->data.dept, 
            newNode->data.appointmentDate, newNode->data.appointmentTime);
    } else {
        printf("现场挂号成功！挂号编号：%s，患者：%s，医生：%s，科室：%s，挂号日期：%s\n",
            newNode->data.regNo, newNode->data.patientName,
            newNode->data.doctorName, newNode->data.dept, newNode->data.date);
    }
    
    return 1;
}
```

### 3.2 预约挂号界面实现

```c
/**
 * 预约挂号界面
 * 由患者操作，用于预约未来的就诊时间
 */
void patientAppointmentRegistration() {
    char patientCardNo[20], patientName[50];
    char doctorEmpNo[20], doctorName[50], dept[50];
    char appointmentDate[20], appointmentTime[20];
    int success;
    
    printf("=== 患者预约挂号 ===\n");
    
    // 1. 获取患者信息
    printf("请输入您的卡号: ");
    scanf("%s", patientCardNo);
    
    // 从患者链表中获取患者姓名
    Patient* patient = findPatientByCardNo(getPatientHead(), patientCardNo);
    if(patient == NULL) {
        printf("未找到对应患者信息\n");
        return;
    }
    strcpy(patientName, patient->data.name);
    
    // 2. 获取医生信息
    printf("请输入医生工号: ");
    scanf("%s", doctorEmpNo);
    
    // 从医生链表中获取医生信息
    Doctor* doctor = findDoctorByEmpNo(getDoctorHead(), doctorEmpNo);
    if(doctor == NULL) {
        printf("未找到对应医生\n");
        return;
    }
    strcpy(doctorName, doctor->data.name);
    strcpy(dept, doctor->data.dept);
    
    // 3. 获取预约日期
    printf("请输入预约日期 (YYYY-MM-DD，如今天请输入 %04d-%02d-%02d): ", 
           g_currentYear, g_currentMonth, g_currentDay);
    scanf("%s", appointmentDate);
    
    // 4. 验证日期有效性
    int year, month, day;
    if(sscanf(appointmentDate, "%d-%d-%d", &year, &month, &day) != 3) {
        printf("日期格式错误\n");
        return;
    }
    
    if(!isValidDate(year, month, day)) {
        printf("无效的日期\n");
        return;
    }
    
    // 5. 检查预约日期不能早于今天
    if(compareDate(year, month, day, g_currentYear, g_currentMonth, g_currentDay) < 0) {
        printf("预约日期不能早于今天\n");
        return;
    }
    
    // 6. 获取预约时间
    printf("请输入预约时间 (HH:MM，如 09:00): ");
    scanf("%s", appointmentTime);
    
    // 7. 验证时间格式
    int hour, minute;
    if(sscanf(appointmentTime, "%d:%d", &hour, &minute) != 2) {
        printf("时间格式错误\n");
        return;
    }
    
    if(hour < 0 || hour > 23 || minute < 0 || minute > 59) {
        printf("时间无效\n");
        return;
    }
    
    // 8. 执行预约挂号
    success = addRegistration(&g_registrationHead, &g_registrationTail,
                             patientCardNo, patientName,
                             doctorEmpNo, doctorName, dept,
                             appointmentDate, appointmentTime, PATIENT);
    
    if(success) {
        printf("预约挂号成功！请按时就诊。\n");
    } else {
        printf("预约挂号失败！\n");
    }
}
```

### 3.3 现场挂号实现

```c
/**
 * 现场挂号界面
 * 由护士操作，用于当天即时挂号
 */
void nurseOnsiteRegistration() {
    char patientCardNo[20], patientName[50];
    char doctorEmpNo[20], doctorName[50], dept[50];
    char currentDate[20], currentTime[20];
    int success;
    
    printf("=== 现场挂号 ===\n");
    
    // 1. 获取患者信息
    printf("请输入患者卡号: ");
    scanf("%s", patientCardNo);
    
    // 从患者链表中获取患者姓名
    Patient* patient = findPatientByCardNo(getPatientHead(), patientCardNo);
    if(patient == NULL) {
        printf("未找到对应患者信息\n");
        return;
    }
    strcpy(patientName, patient->data.name);
    
    // 2. 获取医生信息
    printf("请输入医生工号: ");
    scanf("%s", doctorEmpNo);
    
    // 从医生链表中获取医生信息
    Doctor* doctor = findDoctorByEmpNo(getDoctorHead(), doctorEmpNo);
    if(doctor == NULL) {
        printf("未找到对应医生\n");
        return;
    }
    strcpy(doctorName, doctor->data.name);
    strcpy(dept, doctor->data.dept);
    
    // 3. 设置为当天日期和时间
    int year, month, day, hour, minute, second;
    time_t now = time(0);
    struct tm* timeinfo = localtime(&now);
    
    year = timeinfo->tm_year + 1900;
    month = timeinfo->tm_mon + 1;
    day = timeinfo->tm_mday;
    hour = timeinfo->tm_hour;
    minute = timeinfo->tm_min;
    second = timeinfo->tm_sec;
    
    sprintf(currentDate, "%04d-%02d-%02d", year, month, day);
    sprintf(currentTime, "%02d:%02d", hour, minute);
    
    // 4. 执行现场挂号
    success = addRegistration(&g_registrationHead, &g_registrationTail,
                            patientCardNo, patientName,
                            doctorEmpNo, doctorName, dept,
                            currentDate, currentTime, NURSE);
    
    if(success) {
        printf("现场挂号成功！请患者等待叫号。\n");
    } else {
        printf("现场挂号失败！\n");
    }
}
```

### 3.4 患者查看个人挂号记录

```c
/**
 * 患者查看个人挂号记录
 */
void patientViewOwnRegistrations(char* patientCardNo) {
    Registration* head = getRegistrationHead();
    Registration* current = head;
    int count = 0;
    
    printf("=== 您的挂号记录 ===\n");
    printf("%-15s %-15s %-15s %-15s %-12s %-10s %-10s %-8s\n",
           "挂号编号", "医生姓名", "科室", "挂号日期", "预约时间", "挂号时间", "状态", "挂号方式");
    
    while(current != NULL) {
        if(strcmp(current->data.patientCardNo, patientCardNo) == 0) {
            // 显示挂号信息
            char statusStr[20];
            switch(current->data.status) {
                case REG_PENDING: strcpy(statusStr, "待就诊"); break;
                case REG_IN_PROGRESS: strcpy(statusStr, "就诊中"); break;
                case REG_COMPLETED: strcpy(statusStr, "已完成"); break;
                case REG_CANCELLED: strcpy(statusStr, "已取消"); break;
                default: strcpy(statusStr, "未知");
            }
            
            char createdByStr[10];
            switch(current->data.createdBy) {
                case PATIENT: strcpy(createdByStr, "预约"); break;
                case NURSE: strcpy(createdByStr, "现场"); break;
                default: strcpy(createdByStr, "系统");
            }
            
            printf("%-15s %-15s %-15s %-15s %-12s %-10s %-10s %-8s\n",
                   current->data.regNo,
                   current->data.doctorName,
                   current->data.dept,
                   current->data.date,
                   current->data.appointmentTime,
                   current->data.time,
                   statusStr,
                   createdByStr);
            count++;
        }
        current = current->next;
    }
    
    if(count == 0) {
        printf("您目前没有挂号记录\n");
    } else {
        printf("总计: %d 条记录\n", count);
    }
}
```

### 3.5 患者取消个人挂号

```c
/**
 * 患者取消个人挂号
 */
int patientCancelRegistration(char* patientCardNo, char* regNo) {
    Registration* head = getRegistrationHead();
    Registration* current = head;
    
    // 查找指定的挂号记录
    while(current != NULL) {
        if(strcmp(current->data.patientCardNo, patientCardNo) == 0 &&
           strcmp(current->data.regNo, regNo) == 0) {
            break;
        }
        current = current->next;
    }
    
    if(current == NULL) {
        printf("未找到对应的挂号记录\n");
        return 0;
    }
    
    // 检查状态，只有待就诊的挂号才能取消
    if(current->data.status != REG_PENDING) {
        printf("只有待就诊状态的挂号才能取消\n");
        return 0;
    }
    
    // 查找对应医生并减少其当前患者数量（如果是当天的挂号）
    if(strcmp(current->data.appointmentDate, current->data.date) == 0) {
        Doctor* d = findDoctorByEmpNo(getDoctorHead(), current->data.doctorEmpNo);
        if(d != NULL && d->data.currentPatients > 0) {
            d->data.currentPatients--;
        }
    }
    
    // 修改挂号记录状态为已取消
    current->data.status = REG_CANCELLED;
    
    // 更新挂号记录文件
    rebuildRegistrationFile(head);
    printf("挂号取消成功\n");
    return 1;
}
```

### 3.6 医生叫号功能

```c
/**
 * 医生叫号功能
 */
int callNextPatient(Registration** head, Registration** tail, 
                   char* doctorEmpNo, UserType user_type) {
    if (head == NULL || *head == NULL) {
        printf("暂无挂号记录\n");
        return 0;
    }
    
    Registration* current = *head;
    Registration* target = NULL;
    
    // 查找指定医生的第一个待就诊患者
    while (current != NULL) {
        if (strcmp(current->data.doctorEmpNo, doctorEmpNo) == 0 &&
            current->data.status == REG_PENDING) {
            target = current;
            break;
        }
        current = current->next;
    }
    
    if (target == NULL) {
        printf("该医生暂无待就诊患者\n");
        return 0;
    }
    
    // 修改状态为"就诊中"
    target->data.status = REG_IN_PROGRESS;
    printf("请患者 %s 到 %s 诊室就诊\n", target->data.patientName, 
           target->data.doctorName);
    
    // 更新文件
    rebuildRegistrationFile(*head);
    return 1;
}
```

### 3.7 护士管理挂号功能

```c
/**
 * 护士查看科室挂号列表
 */
void nurseViewDepartmentRegistrations(char* dept) {
    Registration* head = getRegistrationHead();
    Registration* current = head;
    int count = 0;
    
    printf("=== %s 科室挂号列表 ===\n", dept);
    printf("%-15s %-15s %-15s %-15s %-12s %-10s %-10s\n",
           "挂号编号", "患者姓名", "医生姓名", "挂号日期", "预约时间", "挂号时间", "状态");
    
    while(current != NULL) {
        if(strcmp(current->data.dept, dept) == 0) {
            // 显示挂号信息
            char statusStr[20];
            switch(current->data.status) {
                case REG_PENDING: strcpy(statusStr, "待就诊"); break;
                case REG_IN_PROGRESS: strcpy(statusStr, "就诊中"); break;
                case REG_COMPLETED: strcpy(statusStr, "已完成"); break;
                case REG_CANCELLED: strcpy(statusStr, "已取消"); break;
                default: strcpy(statusStr, "未知");
            }
            
            printf("%-15s %-15s %-15s %-15s %-12s %-10s %-10s\n",
                   current->data.regNo,
                   current->data.patientName,
                   current->data.doctorName,
                   current->data.date,
                   current->data.appointmentTime,
                   current->data.time,
                   statusStr);
            count++;
        }
        current = current->next;
    }
    
    if(count == 0) {
        printf("该科室目前没有挂号记录\n");
    } else {
        printf("总计: %d 条记录\n", count);
    }
}
```

## 4. 权限控制集成

### 4.1 在挂号操作中加入权限检查

```c
/**
 * 带权限检查的挂号函数
 */
int secureAddRegistration(Registration** head, Registration** tail,
                        char* patientCardNo, char* patientName,
                        char* doctorEmpNo, char* doctorName, char* dept,
                        char* appointmentDate, char* appointmentTime, 
                        UserType creator, UserType currentUserType) {
    
    // 检查当前用户是否有挂号权限
    if (!check_permission(currentUserType, "make_appointment")) {
        printf("权限不足，无法进行挂号操作！\n");
        return 0;
    }
    
    // 检查如果是患者挂号，只能为自己挂号
    if (currentUserType == PATIENT && 
        strcmp(patientCardNo, getCurrentPatientCardNo()) != 0) {
        printf("患者只能为自己挂号！\n");
        return 0;
    }
    
    // 调用实际的挂号函数
    return addRegistration(head, tail, patientCardNo, patientName,
                         doctorEmpNo, doctorName, dept,
                         appointmentDate, appointmentTime, creator);
}
```

## 5. 主要功能菜单

### 5.1 患者挂号菜单

```c
void patientRegistrationMenu() {
    int choice;
    
    do {
        printf("\n=== 患者挂号菜单 ===\n");
        printf("1. 预约挂号\n");
        printf("2. 查看我的挂号记录\n");
        printf("3. 取消我的挂号\n");
        printf("0. 返回主菜单\n");
        printf("请选择: ");
        scanf("%d", &choice);
        
        switch(choice) {
            case 1:
                patientAppointmentRegistration();
                break;
            case 2:
                // 需要先获取当前患者卡号
                {
                    char cardNo[20];
                    printf("请输入您的卡号: ");
                    scanf("%s", cardNo);
                    patientViewOwnRegistrations(cardNo);
                }
                break;
            case 3:
                // 取消挂号
                {
                    char cardNo[20], regNo[20];
                    printf("请输入您的卡号: ");
                    scanf("%s", cardNo);
                    printf("请输入要取消的挂号编号: ");
                    scanf("%s", regNo);
                    patientCancelRegistration(cardNo, regNo);
                }
                break;
            case 0:
                printf("返回主菜单\n");
                break;
            default:
                printf("无效选择，请重新输入\n");
                break;
        }
    } while(choice != 0);
}
```

### 5.2 护士挂号菜单

```c
void nurseRegistrationMenu() {
    int choice;
    
    do {
        printf("\n=== 护士挂号菜单 ===\n");
        printf("1. 现场挂号\n");
        printf("2. 查看科室挂号列表\n");
        printf("3. 协助叫号\n");
        printf("0. 返回主菜单\n");
        printf("请选择: ");
        scanf("%d", &choice);
        
        switch(choice) {
            case 1:
                nurseOnsiteRegistration();
                break;
            case 2:
                // 需要获取护士所在科室
                {
                    char dept[50];
                    printf("请输入科室名称: ");
                    scanf("%s", dept);
                    nurseViewDepartmentRegistrations(dept);
                }
                break;
            case 3:
                // 叫号功能
                {
                    char doctorEmpNo[20];
                    printf("请输入医生工号: ");
                    scanf("%s", doctorEmpNo);
                    callNextPatient(&g_registrationHead, &g_registrationTail, 
                                   doctorEmpNo, NURSE);
                }
                break;
            case 0:
                printf("返回主菜单\n");
                break;
            default:
                printf("无效选择，请重新输入\n");
                break;
        }
    } while(choice != 0);
}
```

## 6. 数据验证和安全措施

### 6.1 输入验证

- 验证患者卡号的有效性
- 验证医生工号的存在性
- 验证日期和时间的合理性
- 防止重复挂号
- 检查医生号源是否充足

### 6.2 权限验证

- 确保患者只能为自己挂号
- 确保护士只能为患者现场挂号
- 确保医生只能管理自己的患者
- 确保操作符合角色权限

## 7. 文件存储格式

挂号记录文件格式：
```
1
REG20260407150001    ← 挂号单号
PT001                ← 病人卡号
张三                 ← 病人姓名
DOC001               ← 医生工号
刘医生               ← 医生姓名
内科                 ← 科室
2026-04-07          ← 挂号日期
15:30:00            ← 挂号时间
2026-04-07          ← 预约日期
16:00               ← 预约时间
0                   ← 状态（0-待就诊）
0                   ← 挂号方式（0-预约, 1-现场）

-1                   ← 结束标记
```

通过以上实现，系统完整支持预约挂号和现场挂号两种方式，并根据4种身份（患者、护士、医生、管理员）进行相应的权限控制。