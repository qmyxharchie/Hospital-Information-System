# 医院综合信息管理系统 - 团队开发协作指南

> 适用项目：吉林大学《程序设计基础课程设计》
> 团队规模：3人
> 核心语言：C语言（GUI层用C++ StellarX框架）

---

## 一、团队分工

| 组员 | 负责模块 | 源文件 | 数据文件 | 优先级 |
|------|---------|--------|---------|--------|
| 组长（你） | 病人 + 医生 + 挂号 | patient.c/h、doctor.c/h、registration.c/h | patient.txt、doctor.txt、registration.txt | ★★★ 最先完成（挂号依赖其他两个） |
| 成员2 | 登录 + 文件IO + 工具函数 | login.c/h、file_io.c/h、utils.c/h | user.txt、所有数据文件的读写 | ★★ 最早完成（基础设施，其他模块都依赖） |
| 成员3 | 药品 + 住院 + 床位 + GUI | medicine.c/h、hospitalization.c/h、bed.c/h、main_gui.cpp | medicine.txt、purchase.txt、hospitalization.txt、bed.txt | ★ 收尾（整合所有模块） |

---

## 二、协调机制

### 2.1 约定1：文件存放位置

所有人在各自的电脑上保持完全一致的目录结构。

```
hospital-management/           ← 项目根目录（三人共享同一个Git仓库）
│
├── CMakeLists.txt            ← 组长维护（添加新文件时同步）
│
├── include/                  ← 头文件目录
│   ├── StellarX/            ← StellarX图形库（不动）
│   ├── patient.h            ← 组长写
│   ├── doctor.h             ← 组长写
│   ├── registration.h       ← 组长写
│   ├── medicine.h           ← 成员3写
│   ├── hospitalization.h    ← 成员3写
│   ├── bed.h                ← 成员3写
│   ├── login.h              ← 成员2写
│   ├── file_io.h            ← 成员2写
│   ├── utils.h              ← 成员2写
│   └── ui.h                 ← 成员3写
│
├── src/                     ← 源文件目录
│   ├── StellarX/            ← StellarX图形库（不动）
│   ├── main_gui.cpp         ← 成员3写（最后整合）
│   ├── patient.c             ← 组长写
│   ├── doctor.c              ← 组长写
│   ├── registration.c       ← 组长写
│   ├── medicine.c            ← 成员3写
│   ├── hospitalization.c     ← 成员3写
│   ├── bed.c                 ← 成员3写
│   ├── login.c              ← 成员2写
│   ├── file_io.c            ← 成员2写
│   ├── utils.c              ← 成员2写
│   └── ui.c                 ← 成员3写
│
├── data/                    ← 数据文件（运行时生成）
│   ├── patient.txt
│   ├── doctor.txt
│   ├── registration.txt
│   ├── medicine.txt
│   ├── purchase.txt
│   ├── hospitalization.txt
│   ├── bed.txt
│   └── user.txt
│
├── backup/                  ← 自动备份
│
├── bin/                     ← 编译输出
│
└── docs/                    ← 文档目录
```

**每个人严格按照这个结构放文件，不要擅自创建新目录。**

---

### 2.2 约定2：函数命名规范

所有模块统一使用以下命名规则，**任何人不得擅自更改**。

#### 链表操作函数

| 函数名 | 说明 | 示例 |
|--------|------|------|
| `buildXxxChain` | 从文件加载数据到链表 | `buildPatientChain(Patient** head, Patient** tail)` |
| `addXxx` | 尾部添加节点 | `addPatient(Patient** head, Patient** tail, ...)` |
| `delXxx` | 删除节点 | `delPatient(Patient** head, Patient** tail, Patient* p)` |
| `findXxxByXxx` | 按某字段查找 | `findPatientByCardNo(Patient* head, char* cardNo)` |
| `listAllXxx` | 遍历显示所有 | `listAllPatients(Patient* head)` |
| `rebuildXxxFile` | 链表内容写回文件 | `rebuildPatientFile(Patient* head)` |
| `freeXxxChain` | 释放所有节点内存 | `freePatientChain(Patient* head)` |

#### 全局变量命名

| 变量名 | 说明 | 定义位置 |
|--------|------|---------|
| `g_patientHead` | 病人链表头指针 | `patient.c`（static） |
| `g_patientTail` | 病人链表尾指针 | `patient.c`（static） |
| `g_doctorHead` | 医生链表头指针 | `doctor.c`（static） |
| `g_currUser` | 当前登录用户 | `login.c`（static） |

**重要**：所有链表头指针在本 `.c` 文件内部用 `static` 定义，不暴露给其他文件。如果其他模块需要访问，调用对应的 `getXxxHead()` 函数。

#### 跨模块调用方式

**方式A：通过参数传递（推荐）**

```c
// 挂号函数签名（组长负责）
int addRegistration(Registration** head, Registration** tail,
                    Patient* patientHead, Doctor* doctorHead,
                    char* patientCardNo, char* doctorEmpNo, char* date);

// main_gui.cpp 调用时：
addRegistration(&g_regHead, &g_regTail,
                getPatientHead(), getDoctorHead(),
                cardNo, empNo, date);
```

**方式B：提供getter函数（成员2负责utils.h）**

```c
// 在utils.h中统一提供getter声明
#ifndef _UTILS_H_
#define _UTILS_H_

// 获取各模块链表头指针的getter函数声明
extern Patient* getPatientHead();
extern Patient* getPatientTail();
extern Doctor* getDoctorHead();
extern Doctor* getDoctorTail();
// ... 其他模块同理

#endif
```

**本项目推荐方式A**（更清晰，依赖关系一目了然），每条函数调用都显式传入需要用到的数据。

---

### 2.3 约定3：Git分支与协作流程

#### Git仓库初始化（组长做一次）

```bash
# 组长在GitHub创建空仓库后：
git init
git add CMakeLists.txt include/ src/ data/ docs/ image/
git commit -m "chore: initial project structure"
git branch -M main
git remote add origin https://github.com/组长用户名/hospital-management.git
git push -u origin main
```

#### 每个人创建自己的分支

```bash
# 组长：
git checkout -b feature/patient-doctor-registration

# 成员2：
git checkout -b feature/login-fileio-utils

# 成员3：
git checkout -b feature/medicine-hospital-bed-gui
```

#### 每天开发流程

```bash
# 早上开始工作前：
git checkout feature/你的分支名
git pull origin main   # 拉取最新代码（可能有人合并了新代码）

# 编写代码...

# 每天至少提交一次：
git add .
git commit -m "feat(patient): add addPatient and findPatientByCardNo"
git push origin feature/你的分支名
```

#### 合并代码流程（每阶段完成后）

```bash
# 1. 成员各自在自己的分支完成开发
# 2. 组长在GitHub上创建 Pull Request
# 3. 其他人 review 后，组长合并到 main
# 4. 合并后，所有人重新从 main 创建新分支（可选）
```

#### Commit 提交信息规范

格式：`type(module): 具体改动`

```
feat(patient): add addPatient function
feat(doctor): add doctor CRUD operations  
fix(registration): fix memory leak in addRegistration
docs: update README with build instructions
chore: update CMakeLists.txt for new files
refactor(file_io): rename buildXxxChain parameters
```

---

### 2.4 约定4：CMakeLists.txt 维护规则

**组长负责维护**，任何人新增 `.c` 文件必须通知组长更新 CMakeLists.txt。

```cmake
# CMakeLists.txt 示例结构
cmake_minimum_required(VERSION 3.10)
project(HospitalManagement C CXX)

set(CMAKE_C_STANDARD 99)
set(CMAKE_CXX_STANDARD 17)

# 查找EasyX（Windows专用）
find_package(EasyX QUIET)
if(NOT EasyX_FOUND)
    # 如果找不到，提供替代方案或警告
endif()

# 添加可执行文件
add_executable(hospital_management
    src/main_gui.cpp
    src/patient.c
    src/doctor.c
    src/registration.c
    src/medicine.c
    src/hospitalization.c
    src/bed.c
    src/login.c
    src/file_io.c
    src/utils.c
    src/ui.c
    src/StellarX/window.cpp
    src/StellarX/Button.cpp
    # ... 其他StellarX源文件
)

# 包含头文件目录
target_include_directories(hospital_management PRIVATE
    ${CMAKE_SOURCE_DIR}/include
    ${CMAKE_SOURCE_DIR}/include/StellarX
)
```

---

## 三、开发阶段详细步骤

### 阶段1：基础设施搭建（第1天）

**执行者：全员并行**

#### 成员2（最先完成）

负责 `login.c/h`、`file_io.c/h`、`utils.c/h`

**具体任务**：

```
任务1：utils.h + utils.c
  - generateUniqueId()           ← 生成唯一ID（PT/DOC/REG/MED/HOS前缀+时间戳）
  - getCurrentTime()             ← 获取当前时间字符串
  - getCurrentDate()              ← 获取当前日期字符串
  - safeStringCopy()             ← 安全字符串复制（防溢出）
  - trim()                       ← 去除首尾空白字符
  - isValidPhone()               ← 手机号验证（11位数字）
  - isValidIdCard()              ← 身份证验证（18位）
  - ensureDirectoriesExist()    ← 确保data/backup/bin/image目录存在

任务2：file_io.h + file_io.c
  - buildPatientChain(Patient**, Patient**)
  - rebuildPatientFile(Patient*)
  - buildDoctorChain(Doctor**, Doctor**)
  - rebuildDoctorFile(Doctor*)
  - buildRegistrationChain(Registration**, Registration**)
  - rebuildRegistrationFile(Registration*)
  - buildMedicineChain(Medicine**, Medicine**)
  - rebuildMedicineFile(Medicine*)
  - buildBedChain(Bed**, Bed**)
  - rebuildBedFile(Bed*)
  - buildHospitalizationChain(Hospitalization**, Hospitalization**)
  - rebuildHospitalizationFile(Hospitalization*)
  - buildUserChain(User**, User**)
  - rebuildUserFile(User*)
  - backupAllData()

任务3：login.h + login.c
  - md5Hash(char* input, char* output)  ← 密码哈希（可直接用现成代码）
  - login(char username[], char password[])
  - registerUser(char username[], char password[], int role)
  - getCurrentUser(char* username, int* role)  ← 获取当前登录用户信息
```

**产出物**：`utils.h/c`、`file_io.h/c`、`login.h/c`，共6个文件。

---

#### 组长（等成员2完成utils.h后开始）

负责 `patient.c/h`、`doctor.c/h`

**具体任务**：

```
任务1：patient.h + patient.c
  - Patient结构体定义（cardNo, name, gender, idCard, phone, isActive, next, pre）
  - buildPatientChain()
  - addPatient()
  - delPatient()
  - modifyPatient()
  - findPatientByCardNo()
  - findPatientByName()
  - listAllPatients()
  - rebuildPatientFile()
  - freePatientChain()

任务2：doctor.h + doctor.c
  - Doctor结构体定义（empNo, name, dept, schedule, maxPatients, currentPatients, next, pre）
  - buildDoctorChain()
  - addDoctor()
  - delDoctor()
  - modifyDoctor()
  - findDoctorByEmpNo()
  - findDoctorByName()
  - findDoctorsByDept()
  - listAllDoctors()
  - resetDailyPatients()
  - rebuildDoctorFile()
  - freeDoctorChain()
```

**前置依赖**：需要 `file_io.c` 提供 `buildPatientChain` 和 `rebuildPatientFile` 的实现，所以成员2的 `file_io.c` 要先完成。

---

#### 成员3（等组长完成基础模块后开始）

负责 `medicine.c/h`、`bed.c/h`、`hospitalization.c/h`

**具体任务**：

```
任务1：medicine.h + medicine.c
  - Medicine结构体（medNo, genericName, brandName, spec, price, stock, minStock, next, pre）
  - Purchase结构体（purNo, patientCardNo, medNo, quantity, totalCost, date, next, pre）
  - buildMedicineChain() + rebuildMedicineFile()
  - buildPurchaseChain() + rebuildPurchaseFile()
  - addMedicine()
  - delMedicine()
  - modifyMedicine()
  - findMedicineByNo()
  - purchaseMedicine()         ← 关键：扣减库存，检查低库存预警
  - replenishStock()
  - checkLowStock()            ← 列出所有低于最低库存的药品
  - listAllMedicines()
  - freeMedicineChain()

任务2：bed.h + bed.c
  - Bed结构体（ward, bedNo, patientCardNo, patientName, status, next, pre）
  - buildBedChain() + rebuildBedFile()
  - addBed()
  - allocateBed()              ← 关键：自动分配空闲床位
  - freeBed()                  ← 关键：出院时释放床位
  - findBedByNo()
  - findBedsByWard()
  - findAvailableBeds()
  - getWardStats()
  - listAllBeds()
  - freeBedChain()

任务3：hospitalization.h + hospitalization.c
  - Hospitalization结构体（recordNo, patientCardNo, patientName, bedNo, prepay, totalCost, admissionDate, status, next, pre）
  - buildHospitalizationChain() + rebuildHospitalizationFile()
  - addHospitalization()        ← 关键：入院登记，自动调用allocateBed分配床位
  - updateTotalCost()          ← 更新住院总费用
  - dischargePatient()         ← 关键：出院结算，计算应退金额，调用freeBed释放床位
  - findHospitalizationByCardNo()
  - listAllHospitalizations()
  - freeHospitalizationChain()
```

**前置依赖**：需要 `bed.c` 的 `allocateBed` 和 `freeBed`，所以 bed.c 要先完成。

---

### 阶段2：挂号模块（组长独有，第5~8天）

**组长负责 `registration.c/h`**

**这是整个项目最复杂的模块，涉及跨模块调用。**

```
任务：registration.h + registration.c

核心函数 addRegistration() 的调用关系：
  addRegistration()
    → findDoctorByEmpNo(g_doctorHead, doctorEmpNo)  ← 调用医生模块
    → 检查 currentPatients < maxPatients
    → findPatientByCardNo(g_patientHead, cardNo)    ← 调用病人模块
    → 生成唯一regNo
    → incrementPatientCount(doctor)                  ← 医生接诊数+1
    → 尾插法插入链表
    → rebuildRegistrationFile()

其他函数：
  - cancelRegistration()     ← 取消时需将医生接诊数-1
  - completeRegistration()    ← 标记为已完成
  - findRegistrationByNo()
  - listAllRegistrations()
  - rebuildRegistrationFile()
  - freeRegistrationChain()
```

**跨模块调用注意点**：
- `registration.c` 需要 `#include "doctor.h"` 和 `#include "patient.h"`
- 医生模块和病人模块的链表头指针通过**参数传入**（推荐方式A）
- 挂号模块本身也是被 main_gui.cpp 调用的模块之一

---

### 阶段3：GUI整合（成员3负责，第10~14天）

**成员3负责 `main_gui.cpp`**

```
整合步骤：

步骤1：在 main_gui.cpp 中 include 所有模块头文件
  #include "patient.h"
  #include "doctor.h"
  #include "registration.h"
  #include "medicine.h"
  #include "bed.h"
  #include "hospitalization.h"
  #include "login.h"

步骤2：定义全局链表头指针
  static Patient* g_patientHead = NULL;
  static Patient* g_patientTail = NULL;
  static Doctor* g_doctorHead = NULL;
  static Doctor* g_doctorTail = NULL;
  // ... 其他模块同理

步骤3：程序启动时，加载所有数据
  buildPatientChain(&g_patientHead, &g_patientTail);
  buildDoctorChain(&g_doctorHead, &g_doctorTail);
  buildRegistrationChain(&g_regHead, &g_regTail);
  buildMedicineChain(&g_medHead, &g_medTail);
  buildBedChain(&g_bedHead, &g_bedTail);
  buildHospitalizationChain(&g_hospHead, &g_hospTail);

步骤4：构建GUI界面
  - 创建TabControl（左侧导航）
  - 每个Tab对应一个Canvas
  - 每个Canvas放Table + TextBox + Button
  - Button绑定对应的模块函数

步骤5：程序退出时，保存所有数据
  rebuildPatientFile(g_patientHead);
  rebuildDoctorFile(g_doctorHead);
  rebuildRegistrationFile(g_regHead);
  rebuildMedicineFile(g_medHead);
  rebuildBedFile(g_bedHead);
  rebuildHospitalizationFile(g_hospHead);

  freePatientChain(g_patientHead);
  // ... 其他free函数
```

**GUI开发顺序建议**：
```
顺序1：先把所有模块的加载和保存写好，确保数据能正常读写
顺序2：做一个最简单的登录界面（用户名+密码+登录按钮）
顺序3：登录成功后显示主界面框架（TabControl + 左侧标签）
顺序4：一个Tab一个Tab地做，每加一个Tab就跑通一次
顺序5：最后美化界面（背景图、颜色、字体）
```

---

### 阶段4：编译调试（第15~18天）

**问题处理原则**：

| 问题类型 | 处理方式 |
|---------|---------|
| 编译错误（语法不对） | 查看报错行，逐个修复 |
| 链接错误（找不到函数） | 检查CMakeLists.txt是否漏加了新文件，检查.h是否include正确 |
| 运行时崩溃 | 用打印调试（printf），看哪一行崩的 |
| 数据读不出来 | 检查data/目录下文件是否存在，格式是否正确（每条记录前有"1"，末尾有"-1"） |
| 内存泄漏（越用越卡） | 检查malloc后是否都有对应的free，rebuild后是否free了旧链表 |

**调试技巧**：

```c
// 在关键函数加打印，方便定位问题
void addPatient(Patient** head, Patient** tail, ...) {
    printf("[DEBUG] addPatient called, name=%s\n", name);
    // ... 函数体
    printf("[DEBUG] addPatient success, cardNo=%s\n", newNode->cardNo);
}
```

---

### 阶段5：答辩准备（第19~21天）

```
任务1：准备答辩PPT（每人讲自己负责的部分）
  - 讲清楚模块功能和数据结构
  - 讲清楚遇到了什么困难，怎么解决的
  - 演示主要功能操作

任务2：代码Code Review
  - 所有人坐一起，互相看代码
  - 检查是否有命名不规范、注释不完整的地方
  - 检查是否有内存泄漏（malloc没free）

任务3：准备可能被问到的问题
  - 双向链表和单向链表的区别？为什么要用双向链表？
  - 文件存储格式是怎么设计的？为什么要用"1"和"-1"标记？
  - 如何保证数据的唯一性？（卡号/工号用时间戳生成）
  - 如果数据文件损坏了怎么办？（backup/目录下有备份）
```

---

## 四、每日站会模板（15分钟）

每天开始前，三人简短同步一下：

```text
格式：
姓名 | 昨天做了什么 | 今天要做什么 | 有没有阻塞问题

示例：
组长 | 完成了patient.c的增删改查 | 开始写doctor.c | 等待成员2的file_io.c完成
成员2 | 完成了utils.c全部函数 | 开始写file_io.c | 无
成员3 | 看完了StellarX文档 | 开始写medicine.c的框架 | 等待patient.h的定义
```

---

## 五、文件头注释模板

每个新创建的 `.c` 和 `.h` 文件，顶部必须包含以下注释：

```c
/**
 * @file 模块名.c / .h
 * @brief 模块功能描述
 * 
 * 功能说明：
 * - 功能点1
 * - 功能点2
 * 
 * 开发思路：
 * 1. 设计点1
 * 2. 设计点2
 * 
 * @author 姓名
 * @date 2026-04-XX
 * @version 1.0
 */
```

---

## 六、关键注意事项

### 1. 不要擅自修改别人的接口

如果发现别人写的函数签名不对，**不要自己改**，而是叫那个人改。因为你改了，他的代码就编译不过了。

### 2. 每次开始工作前先 `git pull`

避免两个人改了同一个文件，然后合并冲突。

### 3. 编译通过后再提交代码

每次 commit 确保代码能 `cmake .. && make` 编译通过。如果编译不过但需要暂存，用 `git stash`。

### 4. data/ 目录不提交到 Git

在 `.gitignore` 里加一行 `data/`，因为 data/ 是运行时生成的，不需要版本控制。backup/ 和 bin/ 也同样忽略。

### 5. 遇到编译/链接问题先查文档

StellarX 的用法看 `docs/11-GUI界面.md`，文件IO的格式看 `docs/12-文件IO.md`。

---

## 七、依赖关系图

```
                    ┌──────────────┐
                    │   main_gui   │
                    │   (成员3)    │
                    └──────┬───────┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
   ┌────▼────┐        ┌────▼────┐        ┌────▼────┐
   │ 病人模块 │        │ 医生模块 │        │ 挂号模块 │
   │ (组长)  │        │ (组长)  │        │ (组长)  │
   └────┬────┘        └────┬────┘        └───┬────┘
        │                  │                  │
        └──────────────────┴──────────────────┘
                           │
                    ┌──────▼───────┐
                    │   file_io    │
                    │  (成员2)      │
                    └──────┬───────┘
                           │
                    ┌──────▼───────┐
                    │    utils     │
                    │  (成员2)      │
                    └──────────────┘
```

---

## 八、联络方式

| 成员 | 负责模块 | Git分支 | 备注 |
|------|---------|---------|------|
| 组长 | patient、doctor、registration | feature/patient-doctor-registration | 负责CMakeLists.txt维护 |
| 成员2 | login、file_io、utils | feature/login-fileio-utils | 最早交付，是基础设施 |
| 成员3 | medicine、hospitalization、bed、main_gui | feature/medicine-hospital-bed-gui | 最后整合 |

---

**最后提醒**：按这个文档执行，基本不会出现"我写完了但别人的代码还没好"或者"代码风格不一致"的问题。遇到任何疑问，先查本文档，再问队友。
